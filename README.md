# PF-Sintering

Python implementation of the phase-field sintering model currently developed in MATLAB.

The initial Python port targets the v64 particle-on-substrate model and the short-contact activation-stress campaign. The first development objective is to preserve the existing physical model and numerical structure while moving the implementation into a testable Python package.
